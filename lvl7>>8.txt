# Level 7 → 8

## Goal
Find the password in `data.txt`, located on the line next to the word "millionth".

## Commands used
```bash
ssh bandit7@bandit.labs.overthewire.org -p 2220
grep millionth data.txt
```

## Explanation
`grep` searches through a file's contents line by line for a matching pattern and prints only the 
matching lines. Instead of manually scanning a large file, `grep millionth data.txt` instantly 
returns the one line containing "millionth" — the password sits right next to it on that same line.

## Password for level 8
VR1ljMayciFxbnUokuQmJFw6QC9VKtub
